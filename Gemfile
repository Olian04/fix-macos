source "https://rubygems.org"

# Dependencies for the local preview renderer (script/preview.rb).
# These mirror what Jekyll uses so the offline preview stays representative.
gem "liquid", "~> 4.0"
gem "kramdown", "~> 2.4"
